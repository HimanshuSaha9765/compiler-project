# CompilerX - Python Dependencies
# Python 3.12 tested

Flask==3.0.3
gunicorn==21.2.0
pytest==8.2.2
pytest-cov==5.0.0
reportlab==4.2.0

# Phase 8.2 - LLM Advisor (optional)
# Groq - RECOMMENDED - fastest, free, no credit card
# IMPORTANT: Groq requires httpx < 0.28, otherwise you get:
# "Client.__init__() got an unexpected keyword argument 'proxies'"
# So we pin httpx explicitly:
httpx==0.27.2
groq==0.11.0
python-dotenv==1.0.1

# Alternative - Google Gemini (uncomment to use)
# google-generativeai==0.7.2
